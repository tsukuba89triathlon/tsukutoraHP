<thead>
  <tr> <!--1列目-->
    <% column = %w{div t_rank name t_time s_time s_rank b_time b_rank split split_rank r_time r_rank} %>
    <% column.each do |col| %>
    <th><%= t(".#{col}")%></th>
    <% end %>
  </tr>
</thead>
