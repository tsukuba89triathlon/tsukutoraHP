<% @page_title = t('.title') %>
<div class="contents">
  <div class="heading">
    <h1><%= @page_title %></h1>
  </div>
  <% if notice %><div id="notice"><%= notice %></div><% end %>
  <div class="caution">
    プロフィール等の関係があるので、すでになくなった学群/研究科についても削除はしないようにしてください。<br/>
    削除機能は、誤って同じ学群/研究科のデータを2つ以上作ってしまった場合などのみに使用してください。
  </div>
  <table>
    <thead>
      <tr>
        <th><%= t('activerecord.attributes.admin/school.school') %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @schools.each do |school| %>
        <tr>
          <td><%= school.school %></td>
          <td><%= link_to t('general.show'), school %></td>
          <td><%= link_to t('general.edit'), edit_admin_school_path(school) %></td>
          <td><%= link_to t('general.delete'), school, method: :delete, data: { confirm: t('general.delete_confirm') } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to "#{t('general.new',{model: "#{t('activerecord.models.admin/school')}"})}", new_admin_school_path, class: 'btn btn-default' %>
</div>
