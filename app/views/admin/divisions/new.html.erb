<% @page_title = t('.title') %>
<div class="contents">
  <div class="heading">
    <h1><%= @page_title %></h1>
  </div>

  <%= render 'form', division: @division %>

  <%= link_to t('general.back'), admin_divisions_path, class: 'btn btn-default' %>
</div>
