<%= form_with(model: lab_seminar, local: true) do |form| %>
  <% if lab_seminar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lab_seminar.errors.count, "error") %> prohibited this lab_seminar from being saved:</h2>

      <ul>
      <% lab_seminar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
