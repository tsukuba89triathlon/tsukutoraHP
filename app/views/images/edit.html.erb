<% @page_title = t('.title') %>
<div class="contents clearfix">
  <div class="heading">
    <h1><%= "#{Member.find(session[:member_id]).full_name}さんの" + @page_title %></h1>
  </div>
  <%= render 'form', image: @image %>
  <%= link_to t('general.back'), account_images_path, class: 'btn btn-default' %>
</div>
